# Aeon Worker Cockpit example config
PORT=7700
MCP_SERVER=claude-team-http
OBSIDIAN_VAULT="$HOME/obsidian-vault"
# Optional: override where the ensure script keeps pid/log state
# WORKER_COCKPIT_STATE_DIR="$HOME/.local/state/aeon-worker-cockpit"
